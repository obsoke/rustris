use ggez::{Context, graphics};

pub struct GameOverState {
    // game_over_text: graphics::Text,
    // final_score_text: graphics::Text,
    // final_line_text: graphics::Text,
    // instruction_text: graphics::Text,
}

impl GameOverState {
    fn new(ctx: &mut Context) -> Self {
        // let game_over = graphics::Text::new(ctx, "GAME OVER", &font_big)?;

        // let instruction_src = "'R' to restart / 'M' for menu / 'Esc' to quit";
        // let instruction_text = graphics::Text::new(ctx, instruction_src, &font_small)?;
        // let final_score = graphics::Text::new(ctx, "Final Score", &font_small)?;
        // let final_lines = graphics::Text::new(ctx, "Final Lines", &font_small)?;
        // GameOverState {
        //     game_over_text: game_over,
        //     final_score_text: final_score,
        //     final_line_text: final_lines,
        //     instruction_text: instruction_text,
        // }
        GameOverState {}
    }
}
